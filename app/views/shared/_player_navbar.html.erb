<nav class="navbar navbar-expand-lg navbar-dark bg-rugby-purple mb-4">
  <div class="container">
    <div class="h4 fw-bold text-white mb-0">
      BreakDown<span class="text-danger">Lab</span>
    </div>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav mx-auto flex-row gap-0">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to matches_path, class: "nav-link text-white #{'active' if controller_name == 'matches'}" do %>
              Matches
            <% end %>
          </li>
          <li class="nav-item">
            <%= link_to team_profile_path, class: "nav-link text-white #{'active' if controller.class.name.start_with?('Teams') && action_name == 'team_profile'}" do %>
              Team Profile
            <% end %>
          </li>
          <li class="nav-item">
            <%= link_to profile_path, class: "nav-link text-white #{'active' if controller_name == 'players' && controller.action_name == 'profile'}" do %>
              My Profile
            <% end %>
          </li>
          <li class="nav-item">
            <%= link_to head_to_head_path,
                class: "nav-link text-white #{'active' if controller.action_name == 'head_to_head'}" do %>
              H2H
            <% end %>
          </li>
        <% end %>
      </ul>

      <ul class="navbar-nav">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= button_to destroy_user_session_path, method: :delete, class: "btn btn-link nav-link text-white p-0 border-0 bg-transparent" do %>
              <i class="fa fa-sign-out"></i> <%= current_user.name %>
            <% end %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to new_user_session_path, class: "nav-link text-white" do %>
              <i class="fas fa-sign-in-alt me-1"></i> Sign In
            <% end %>
          </li>
          <li class="nav-item">
            <%= link_to new_user_registration_path, class: "nav-link text-white" do %>
              <i class="fas fa-user-plus me-1"></i> Sign Up
            <% end %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
