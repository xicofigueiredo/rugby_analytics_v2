<nav class="navbar navbar-expand-lg navbar-dark bg-rugby-purple mb-4">
  <div class="container">
    <div class="h4 fw-bold text-white mb-0 ms-3">
      BreakDown<span class="text-danger">Lab</span>
    </div>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav mx-auto flex-row gap-0">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to matches_path, class: "nav-link text-white #{'active' if controller_name == 'matches'}" do %>
              Matches
            <% end %>
          </li>
          <li class="nav-item">
            <%= link_to teams_path, class: "nav-link text-white #{'active' if controller_name == 'teams' && action_name == 'show'}" do %>
              My Club
            <% end %>
          </li>
          <li class="nav-item">
            <%= link_to team_profile_path,
                class: "nav-link text-white #{'active' if controller.class.name.start_with?('Teams') && action_name == 'team_profile'}" do %>
              My Team
            <% end %>
          </li>
          <li class="nav-item">
            <%= link_to head_to_head_path,
                class: "nav-link text-white #{'active' if controller.action_name == 'head_to_head'}",
                data: { turbo_frame: "main_content" } do %>
              H2H
            <% end %>
          </li>
          <li class="nav-item">
            <%= button_to destroy_user_session_path, method: :delete, class: "btn btn-outline-light ms-3" do %>
              <i class="fas fa-sign-out-alt me-1"></i>
            <% end %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to new_user_session_path, class: "btn btn-outline-light" do %>
              <i class="fas fa-sign-in-alt me-1"></i> Sign In
            <% end %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
