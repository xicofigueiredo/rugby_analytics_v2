<nav class="navbar navbar-expand-lg navbar-dark bg-rugby-purple mb-4">
  <div class="container">
    <div class="h4 fw-bold text-white mb-0 ms-3">
      BreakDown<span class="text-danger">Lab</span>
    </div>

    <button class="navbar-toggler d-none d-lg-block" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="ms-auto d-lg-none">
      <% if user_signed_in? %>
        <%= button_to destroy_user_session_path, method: :delete, class: "btn btn-outline-light" do %>
          <i class="fas fa-sign-out-alt me-1"></i>
        <% end %>
      <% else %>
        <%= link_to new_user_session_path, class: "btn btn-outline-light" do %>
          <i class="fas fa-sign-in-alt me-1"></i> Sign In
        <% end %>
      <% end %>
    </div>

    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav mx-auto flex-row gap-0 d-none d-lg-flex">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to matches_path, class: "nav-link text-white #{'active' if controller_name == 'matches'}" do %>
              Matches
            <% end %>
          </li>
          <li class="nav-item">
            <%= link_to teams_path, class: "nav-link text-white #{'active' if controller_name == 'teams'}" do %>
              Teams
            <% end %>
          </li>
          <li class="nav-item">
            <%= link_to players_path, class: "nav-link text-white #{'active' if controller_name == 'players' && controller.action_name != 'head_to_head' && controller.action_name != 'profile'}" do %>
              Players
            <% end %>
          </li>
          <li class="nav-item">
            <%= link_to admin_users_path, class: "nav-link text-white #{'active' if controller.class.name.start_with?('Admin::')}" do %>
              Users
            <% end %>
          </li>
          <li class="nav-item">
            <%= link_to team_profile_path, class: "nav-link text-white #{'active' if controller.class.name.start_with?('Teams') && action_name == 'team_profile'}" do %>
              My Team
            <% end %>
          </li>
          <li class="nav-item">
            <%= link_to profile_path, class: "nav-link text-white #{'active' if controller_name == 'players' && controller.action_name == 'profile'}" do %>
              My Profile
            <% end %>
          </li>
          <li class="nav-item">
            <%= link_to head_to_head_path,
                class: "nav-link text-white #{'active' if controller.action_name == 'head_to_head'}" do %>
              H2H
            <% end %>
          </li>
        <% end %>
      </ul>

      <ul class="navbar-nav me-3 d-none d-lg-flex">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= button_to destroy_user_session_path, method: :delete, class: "btn btn-outline-light" do %>
              <i class="fas fa-sign-out-alt me-1"></i>
            <% end %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to new_user_session_path, class: "btn btn-outline-light" do %>
              <i class="fas fa-sign-in-alt me-1"></i> Sign In
            <% end %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
